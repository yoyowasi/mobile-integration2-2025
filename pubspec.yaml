name: mobile_integration2_2025
description: "Adaptive Pomodoro + Stats (Flutter, Riverpod, go_router, sqflite)."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.9.2 <4.0.0"   # ← 가독성 위해 범위 표기(기존 ^3.9.2도 동작은 동일)

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  sqflite: ^2.3.3
  path_provider: ^2.1.4
  flutter_local_notifications: ^17.2.1
  timezone: ^0.9.2
  fl_chart: ^0.68.0
  intl: ^0.20.2          # ← 날짜·숫자 포맷용(권장)
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  # 선택: 에셋/폰트 자동 코드 생성(추가 시 아래 flutter_gen_runner 주석 해제)
  # build_runner: ^2.4.12
  # flutter_gen_runner: ^5.6.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/sounds/
  #   - assets/images/
  # fonts:
  #   - family: Pretendard
  #     fonts:
  #       - asset: assets/fonts/Pretendard-Regular.ttf
  #       - asset: assets/fonts/Pretendard-Bold.ttf
  #         weight: 700
